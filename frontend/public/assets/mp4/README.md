# MP4 assets (preview environment)

This folder is read by the React kiosk in the preview environment.
Drop the same files here AND in `/app/public/assets/mp4/` for production:

* `music.mp4`
* `cleaning.mp4`
